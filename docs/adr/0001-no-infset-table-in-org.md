# org では情報集合表を実体化しない

rnd 側は「木を展開して情報集合表を作る」「表を舐めて必勝・必敗を判定する」の
二相に分離できるが、org 側は意思決定点が 7.7×10¹¹ あり、表を実体化すると実行環境の
メモリ上限を数桁超える。そのため org では表を持たず、展開の途中で判定器を呼ぶ
方式を採る。rnd と org で構造が揃っていないのは意図的であり、揃えようとして
org を二相にしてはならない。
