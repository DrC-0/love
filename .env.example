# このファイルを .env にコピーして、環境ごとの値を入れる。
# .env は gitignore 済みなので、リモートへは scp.sh で別途送ること。

# 集計結果をアップロードする Google スプレッドシートの ID
SPREADSHEET_ID=

# scp.sh の送信先 (ssh の設定名:パス)
# ~ はリモート側で展開させたいので、必ずシングルクォートで囲むこと
REMOTE_TARGET='remote-server:~/love'
