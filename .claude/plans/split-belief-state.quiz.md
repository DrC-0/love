# 設問: Belief State の分割

計画書 `.claude/plans/split-belief-state.md` を読んで答えること。
推測ではなく計画書とコードに基づいて答え、根拠となる箇所を示すこと。
計画書に書かれていないことは「不明」と答えること。

## Q1

`bf_position.hpp` には生配列 (`const int hand[2], const int trash[8], ...`) を取る
自由関数が 8 本ある。このうち削除するのはどれで、残すのはどれか。
**それぞれ関数名を列挙すること。**

## Q2

Q1 で「残す」とした関数について、削除できない理由を述べよ。
特に `bf_position::hand_s_est` がその関数をどう呼んでいるか、
渡している配列とフラグが `bf_position::hand_e` の場合と何が違うかを説明すること。
`_s` と `_e` のどちらの視点の話かを明示すること。

## Q3

この変更は org 側と rnd 側のどちらを触るか。片方だけなら、もう片方を
触らなくてよい理由は何か。`bf_position` において org と rnd はどう切り替わるか。

## Q4

この変更は行動履歴を伸ばす処理を含むか。含むなら、末尾に 1 文字追記するのか、
末尾 1 文字を payload 付きに置換するのか。含まないなら、履歴を扱うコードは
どう処理されるか。

## Q5

分割後、`visit_winlose.hpp` と `infset_iswin.cpp` の `#include` 行は
どう変わるか。変わらないなら、それが成立する仕組みは何か。

## Q6

新しく作る 4 つの `belief_state*.hpp` のうち、`belief_state_lose.hpp` が
include するのはどれか。またその理由は何か。逆向きの依存 (必勝側が必敗側を
呼ぶ) は存在するか。

## Q7

`bs_set.hpp` にも `trash_and_hand_s` と `in` という同名の関数がある。
この変更でそれらをどう扱うか。`endgame.hpp` と `end` ターゲットは
影響を受けるか、受けないか。その根拠は何か。

## Q8

`bf_position::bf_position(int open[3], std::string history, bool rnd)` の
宣言と定義は分割後に別々のファイルへ分かれる。既定引数 (`= true`) は
宣言側と定義側のどちらに書かれているか。分割にあたってこれをどう扱うか。

## Q9

変更後に走らせるコマンドを列挙し、それぞれ何が確認できれば成功かを述べよ。
回帰ハーネスは何項目を比較し、所要時間はどれくらいか。
