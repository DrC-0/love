# 設問: win の出力形式を wininf / loseinf に変える

計画書 `.claude/plans/winlose-record-format.md` だけを読んで答えること。
分からないものは推測せず「不明」と書くこと。

---

**Q1.** この変更は org 側と rnd 側のどちらを触るか。片方だけなら、もう片方を
触らなくてよい理由は何か。

**Q2.** `infset_iswin.cpp` のグローバル変数 `abs_history` と `only_history` は、
この変更のあと残るか、消えるか。そう判断した理由は何か。

**Q3.** 必勝の行を作るとき、`is_win` の戻り値からスロットを決めてはいけない。
なぜか。代わりに何を呼ぶか。

**Q4.** 必敗側で、`is_lose` が返す `lose_action.first` が次の値だったとき、
`lose_rows` に行を何本書くか。書くならその `kind` と `slot` は何か。

- `9`
- `7`（`bs.is_wiz_choice` は偽、`bs.hand_s[0]` が 3、`bs.hand_s[1]` が 7）
- `1`（`bs.is_wiz_choice` が真）

**Q5.** `bs.hand_s[0]` と `bs.hand_s[1]` が同じカードだった情報集合で、必敗側に
どういう問題が起きるか。計画書はそれをどう防げと言っているか。

**Q6.** 出力ファイル 1 行のバイト列の並びを、先頭から順に書け。フラグのバイトの
各ビットが何を表すかも書け。履歴の長さが 300 バイトだったらどうなるか。

**Q7.** この変更が正しいことを、何をもって確かめるか。具体的なコマンドと、
期待される結果を書け。`./regress.sh save` はいつ実行するか。

**Q8.** `compare_abscfr.cpp` をこの変更でどうするか。そうする理由は何か。
`.gitignore` の `comp` の行はどうするか。

**Q9.** 必勝側で、`is_terminated_win` が `{3, 1}` を返す情報集合（`bs.hand_s[0]` が
3、`bs.hand_s[1]` が 8）に来たとする。`win_rows` に行を何本書くか。その `slot` は
何か。`use_win(bs, Card{8})` は何を返すか、そしてなぜそうなるか。
