# 設問: interface をカードに統一する

計画書 `.claude/plans/card-convention.md` を読んで答えること。
推測ではなく計画書とコードに基づいて答え、根拠となる箇所を示すこと。
計画書に書かれていないことは「不明」と答えること。

## Q1

`belief_state_win.hpp:52` の `for(int i = 1; i < 8; i++)` は、どのカードを
何枚ぶん回しているか。変換後のループはどう書くか。そう判断した根拠は何か。

同じ形のループが他に 3 本ある。それぞれどのファイルの何行目で、どの関数の中か。

## Q2

Q1 のループを `for(int card = 1; card < 8; card++)` と書いてしまった場合、
どのカードが誤って含まれ、どのカードが落ちるか。それは解析結果に影響するか。

## Q3

リテラルの数値をアクセサに渡している箇所が 3 つある。それぞれの現在の値、
変更後の値、そしてそれが**どのカードを指しているか**を答えよ。

そのうち 1 つは取り違えると別のカードを見ることになる。どれで、何と取り違えるか。

## Q4

`able_actions` はこの変換の対象に含まれるか。含まれないなら、その理由は何か。
また `able_actions` の中で**変更する箇所**と**1 文字も変えてはいけない箇所**は
それぞれ何か。

## Q5

引数をカードに変える 7 本の関数の中で、`- 1` が新しく必要になる配列アクセスは
どれか。逆に、添字がカードではないので**そのままにする**配列はどれか。
`sol_flag_e` と `hand_s` はどちらに当たるか。

## Q6

`belief_state::deck(int i, int open_card)` の 2 つの引数は、変換後それぞれ
0-origin と 1-origin のどちらになるか。理由も述べよ。

## Q7

この変更は org 側と rnd 側のどちらを触るか。また行動履歴を伸ばす処理を含むか。
含まないなら、履歴に関係するコードはどう扱われるか。

## Q8

`count_work.hpp` のカウンタ名をこの変更で改名してよいか。理由は何か。

## Q9

`belief_state.hpp:154` の `open_e` を変換するとき、ループ変数を `card` に
すると問題が起きる。何が起きるか、どう対処するか。

## Q10

変更後に走らせるコマンドを列挙し、それぞれ何が確認できれば成功かを述べよ。
`i + 1` / `j + 1` の残存数は、変換前後でそれぞれいくつになるはずか。
