# 設問: 判定のメモ化

計画書 `.claude/plans/memoize-judgement.md` を読んで答えること。
推測ではなく計画書とコードに基づいて答え、根拠となる箇所を示すこと。
計画書に書かれていないことは「不明」と答えること。

## Q1

メモ表は関数ごとに分けるのか、1 つにまとめるのか。その理由は何か。
鍵には `belief_state` のどのフィールドを含めるか。1 つでも落とすと何が起きるか。

## Q2

`commentable_bs` が真のときメモをどう扱うか。なぜそうするのか。
このフラグは普段どちらの値で、誰が何のために変えるのか。

## Q3

`belief_state_win_checker` は誰がいつ作るか。`infset_iswin.cpp` で
関数のローカル変数として作ってはいけない理由は何か。

## Q4

`belief_state_lose_checker` は `belief_state_win_checker` とどういう関係を持つか。
逆向きにしてはいけない理由は何か。`use_lose` の中の `draw_win(...)` という
呼び出しは変更後どう書かれるか。

## Q5

メモ化**しない**関数を挙げ、それぞれ理由を述べよ。
`is_win` と `is_lose` はメモ化するか。

## Q6

`CW_BUMP` はメモ化のラッパ側と `_impl` 側のどちらに置くか。理由は何か。
`count_work.hpp` のカウンタ名を変えてよいか。

## Q7

この変更を回帰ハーネスに掛けたとき、12 項目のうち何項目が一致し、何項目が
一致しないと**期待される**か。それぞれ何を意味するか。
どの項目が一致しなかったら実装のミスか。

## Q8

この変更は org 側と rnd 側のどちらを触るか。行動履歴を伸ばす処理を含むか。
`belief_state_history.hpp` は変更されるか。

## Q9

`winlose_visitor` に 2 つのメンバを足すとき、宣言の順序に制約があるか。
あるならどちらが先で、それはなぜか。

## Q10

判定のロジック本体をどう扱うか。`_impl` に移すときに書き換えてよい箇所はあるか。
メモ化後に次のボトルネックになると分かっているものは何か。この変更で
それに手を入れるか。
