# 設問: separate-org-tree

計画書 `.claude/plans/separate-org-tree.md` を読んで答えること。
根拠が計画書に書かれていない設問には「不明」と答えること。推測で埋めないこと。

---

**Q1.** この変更は org 側・rnd 側のどちらを触るか。片方だけなら、もう片方を
触らなくてよい理由は何か。また、変更してはならないファイルを 3 つ以上挙げよ。

---

**Q2.** 判定器が必勝・必敗判定に渡す「行動履歴のキー」は、`node` のどのメンバから
取るか（メンバ名を正確に書くこと）。またこの変更で、行動履歴に対して
「末尾への追記」または「末尾1文字の payload 付き置換」を行うか。

---

**Q3.** `cutting_w` と `cutting_l` は生存範囲が違う。`org_ds_draw` に対応する
一連のフック（`enter_play` / `mid_play` / `leave_play`）のそれぞれで、
`cutting_w` と `cutting_l` に何が起きるかを列挙せよ。
どちらが「意思決定点単位」で、どちらが「分岐単位」か。

---

**Q4.** `enter_wizard` と `enter_soldier` は削減対象マーカーのカウンタを
増減してはならない。理由を述べよ。特に `enter_wizard` については、
呼び出された直後に何が起こりうるかに触れて説明すること。

---

**Q5.** 魔術師（`org_ds_play` の case 5）の分岐で、`enter_wiz_branch(n, to_self)` は
`res_0win` / `res_1win` のどちらの結果を使うか。次の4通りすべてについて、
使う添字（0 か 1）を答えよ。

- `to_self = false`, `is_zero = false`
- `to_self = false`, `is_zero = true`
- `to_self = true`,  `is_zero = false`
- `to_self = true`,  `is_zero = true`

また、山札が尽きていない通常のループとは別に、`enter_wiz_branch(n, false)` が
呼ばれるもう一つの場所はどこか。

---

**Q6.** 兵士（`org_ds_play` の case 1）の推測ループで、`i == n.hand2`（推測が的中）の
とき、判定器のどのフックを呼び、どのフックを呼ばないか。呼ぶもの・呼ばないものを
それぞれ列挙せよ。また、候補が1つもなかった場合に展開器は何をするか。

---

**Q7.** 判定器の中で `rm_bywin` と `rm_bylose` を作るとき、`cutting_w` /
`cutting_l` はどう書くか。またこの計算と、カウンタを増やす処理との間には
どのような順序の制約があるか。守らないと何が壊れるか。

---

**Q8.** 実装後に走らせるコマンドを列挙せよ。ビット一致を確認するとき、
どの部分ゲームで、どの値をどの順に変えて確認するか。
何が一致すれば合格で、何は合格条件に含まれないか。
