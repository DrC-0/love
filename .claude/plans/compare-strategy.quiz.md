# 設問: CFR の平均戦略と必勝・必敗判定を突き合わせる comp

計画書 `.claude/plans/compare-strategy.md` だけを読んで答えること。
分からないものは推測せず「不明」と書くこと。

---

**Q1.** この変更は org 側と rnd 側のどちらを触るか。Makefile に足す行で
**付けてはいけない** `-D` は何か。その理由は何か。`all:` `main:` の行に
足すか。

**Q2.** `str<部分ゲーム>64.bin` の1情報集合ぶんは何バイトで、中身は何か。
σ̄(行動0) はどの式で求めるか。分母が 0 のときどうするか。起動時のサイズ検査は
どういう式か。

**Q3.** ある行が `is_choice_node == true`、`slot == 0` だったとする。
この行に対応する σ̄ を引くには、CFR の行動番号のいくつを見るか。理由を
`do_action` の番号を使って説明せよ。反転を忘れると何が起きるか。

**Q4.** ある行が `is_choice_node == false`、`slot == 1` だったとする。
CFR の行動番号のいくつを見るか。カード使用ノードでは反転が要らない理由は何か。

**Q5.** 手札2枚が同じカードの意思決定点は、`table_infset` に入っているか。
入っていない/いるとしたら、どのファイルのどの仕組みによるか。comp 側で
除外処理が要るか。

**Q6.** 方向1で「違反候補」と数えるのは、必勝の行と必敗の行それぞれについて
どういう条件のときか。同じ履歴に両方のスロットの行があるときはどう扱うか。

**Q7.** `compare_strategy.cpp` に定義しなければならないグローバル変数を挙げよ。
足りないと何が起きるか。情報集合表を作る手順で、順序を変えてはいけない理由は
何か。

**Q8.** 検証で `./regress.sh check` は何項目が `OK` になるべきか。`DIFFER` が
出た場合それは何を意味するか。方向1・方向2の件数が 0 件だったら不合格か。
