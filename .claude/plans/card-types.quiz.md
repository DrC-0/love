# 設問: カードに型を与える

計画書 `.claude/plans/card-types.md` を読んで答えること。
推測ではなく計画書とコードに基づいて答え、根拠となる箇所を示すこと。
計画書に書かれていないことは「不明」と答えること。

## Q1

`MaybeCard` に順序比較 (`<` `>` `<=` `>=`) を定義してよいか。理由は何か。
`open_e() > 1` は変更後どう書かれるか。

## Q2

大臣のルール `bs.hand_s[0] + bs.hand_s[1] >= 12` は変更後どうなるか。
現在のコードは番兵の `0` にどう依存しているか。新しく足す判定は何で、
それを足すと結果は変わるか。

## Q3

`belief_state` のフィールドのうち `MaybeCard` にするのはどれか。
`trash[8]` はどうするか。その理由は何か。

## Q4

判定の戻り値 (`is_win` / `is_terminated_win` / `is_lose`) の型を
`MaybeCard` を使う形に変えるか。理由は何か。

## Q5

メモ鍵 (`belief_state_win_checker::key`) はどう変わるか。ビット幅や詰める順序は
変えてよいか。変えるとどうなるか。

## Q6

`able_actions` はこの変更でどう扱うか。`int base = 40 + card - 1;` と
`int other_i = bs.other_hand_s(card) - 1;` は変更が必要か。必要なら何に変わり、
埋め込まれる数値は変わるか。

## Q7

`hand_s[0]` と `hand_s[1]` で `.value()` の呼び方に違いがあるか。あるなら何か。

## Q8

`belief_state*.hpp` の外で `hand_s` や `open_e()` に触っているファイルと箇所を
挙げよ。それぞれどう変わるか。

## Q9

この変更は org 側と rnd 側のどちらを触るか。`belief_state_history.hpp` は
変更されるか。されるなら何が変わり、何は変わらないか。

## Q10

変更後に走らせるコマンドを列挙し、それぞれ何が確認できれば成功かを述べよ。
回帰ハーネスの決定的カウンタは一致するべきか、しないべきか。理由は何か。
